const state={data:null};const $=s=>document.querySelector(s);const $$=s=>[...document.querySelectorAll(s)];function fmtDate(iso){const d=new Date(iso);return new Intl.DateTimeFormat('zh-CN',{month:'numeric',day:'numeric',hour:'2-digit',minute:'2-digit',hour12:false,timeZone:'Asia/Shanghai'}).format(d)}function esc(s=''){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}function safeUrl(u=''){try{const x=new URL(u,location.href);return /^https?:$/.test(x.protocol)?x.href:''}catch{return''}}function bindImgErrors(){document.querySelectorAll('.js-news-img').forEach(img=>{img.addEventListener('error',()=>{const wrap=img.closest('.news-thumb');if(wrap)wrap.classList.add('img-failed');img.remove()},{once:true})});const hero=$('.js-hero-img');if(hero)hero.addEventListener('error',()=>hero.remove(),{once:true})}function newsCard(n){const img=safeUrl(n.image_url||'');const trust=n.verified?'<span class="badge verified">权威来源</span>':'<span class="badge caution">待交叉核实</span>';const badges=[n.hot?'<span class="badge hot">热点</span>':'',trust,`<span class="badge">${esc(n.category||'新闻')}</span>`].join('');return `<article class="news-card ${img?'':'no-image'}"><div class="news-body"><div class="news-top">${badges}</div><div class="news-title">${esc(n.title)}</div>${n.summary?`<div class="news-summary">${esc(n.summary)}</div>`:''}<div class="news-meta"><span>${esc(n.source||'来源未知')} · ${fmtDate(n.published_at)}</span>${n.url?`<a href="${esc(n.url)}" target="_blank" rel="noopener">来源 ↗</a>`:''}</div></div>${img?`<div class="news-thumb"><img class="js-news-img" src="${esc(img)}" alt="" loading="lazy" referrerpolicy="no-referrer"></div>`:''}</article>`}function renderList(id,arr){$(id).innerHTML=(arr||[]).map(newsCard).join('')||'<div class="empty-card">暂无数据</div>'}function heroCard(x){if(!x)return'<div class="empty-card">暂无头条</div>';const img=safeUrl(x.image_url||'');return `<article class="hero-card"><div class="hero-media">${img?`<img class="js-hero-img" src="${esc(img)}" alt="" referrerpolicy="no-referrer">`:'<div class="hero-fallback"></div>'}</div><div class="hero-content"><div class="hero-kicker"><span class="badge hot">今日头条</span>${x.category?`<span class="badge">${esc(x.category)}</span>`:''}</div><div class="hero-title">${esc(x.title)}</div>${x.summary?`<div class="hero-summary"><span>摘要</span>${esc(x.summary)}</div>`:''}${x.why_it_matters?`<div class="hero-reason"><span>为什么重要</span>${esc(x.why_it_matters)}</div>`:''}<div class="hero-meta"><span>${esc(x.source||'重点新闻')}${x.published_at?' · '+fmtDate(x.published_at):''}</span>${x.url?`<a href="${esc(x.url)}" target="_blank" rel="noopener">查看原报道 ↗</a>`:''}</div></div></article>`}function marketMini(m){const cls=m.change_pct>0?'up':m.change_pct<0?'down':'flat';const arrow=m.change_pct>0?'▲':m.change_pct<0?'▼':'•';return `<div class="market-mini"><div class="market-name">${esc(m.name)}</div><div class="market-price">${esc(m.price)} ${esc(m.unit||'')}</div><div class="market-change ${cls}">${arrow} ${Math.abs(m.change_pct||0).toFixed(2)}%</div></div>`}function marketCard(m){const cls=m.change_pct>0?'up':m.change_pct<0?'down':'flat';const arrow=m.change_pct>0?'▲':m.change_pct<0?'▼':'•';return `<div class="market-card"><div class="name">${esc(m.name)}</div><div class="price">${esc(m.price)} <small>${esc(m.unit||'')}</small></div><div class="change ${cls}">${arrow} ${(m.change_pct||0).toFixed(2)}%</div><div class="sub">${esc(m.symbol||'')} · ${esc(m.note||'延迟行情')}</div></div>`}function render(data){state.data=data;$('#updatedAt').textContent=`最近更新：${fmtDate(data.updated_at)}（北京时间）`;$('#marketTime').textContent=`更新 ${fmtDate(data.markets_updated_at||data.updated_at)}`;const top=data.top5||[];$('#heroStory').innerHTML=heroCard(top[0]);$('#top5').innerHTML=top.slice(0,5).map((x,i)=>`<article class="top-item"><div class="top-num">${i+1}</div><div><div class="top-title">${esc(x.title)}</div>${x.summary?`<div class="top-summary"><span>摘要</span>${esc(x.summary)}</div>`:''}${x.why_it_matters?`<div class="top-reason"><span>为什么重要</span>${esc(x.why_it_matters)}</div>`:''}</div></article>`).join('');renderList('#chinaPreview',(data.sections.china||[]).slice(0,4));renderList('#worldPreview',(data.sections.world||[]).slice(0,4));renderList('#aiPreview',(data.sections.ai||[]).slice(0,4));for(const key of ['china','world','finance','tech','society','ai'])renderList(`#${key}List`,data.sections[key]||[]);$('#marketStrip').innerHTML=(data.markets||[]).slice(0,6).map(marketMini).join('');$('#marketsGrid').innerHTML=(data.markets||[]).map(marketCard).join('');bindImgErrors()}async function load(){$('#updatedAt').textContent='正在读取最新数据…';try{const r=await fetch(`./data/news.json?t=${Date.now()}`);if(!r.ok)throw new Error('读取失败');render(await r.json())}catch(e){$('#updatedAt').textContent='数据读取失败，请稍后刷新'}}$$('.tab').forEach(btn=>btn.addEventListener('click',()=>{$$('.tab').forEach(x=>x.classList.remove('active'));$$('.tab-panel').forEach(x=>x.classList.remove('active'));btn.classList.add('active');document.querySelector(`[data-panel="${btn.dataset.tab}"]`).classList.add('active');window.scrollTo({top:0,behavior:'smooth'})}));$('#refreshBtn').addEventListener('click',load);const now=new Date();$('#todayDate').textContent=new Intl.DateTimeFormat('zh-CN',{year:'numeric',month:'long',day:'numeric',weekday:'long',timeZone:'Asia/Shanghai'}).format(now);load();