const state={data:null};const $=s=>document.querySelector(s);const $$=s=>[...document.querySelectorAll(s)];function fmtDate(iso){const d=new Date(iso);return new Intl.DateTimeFormat('zh-CN',{month:'numeric',day:'numeric',hour:'2-digit',minute:'2-digit',hour12:false,timeZone:'Asia/Shanghai'}).format(d)}function esc(s=''){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}function newsCard(n){const badges=[n.hot?'<span class="badge hot">热点</span>':'',n.verified?'<span class="badge verified">已核实</span>':''].join('');return `<article class="news-card"><div class="news-top">${badges}<span class="badge">${esc(n.category||'新闻')}</span></div><div class="news-title">${esc(n.title)}</div>${n.summary?`<div class="news-summary">${esc(n.summary)}</div>`:''}<div class="news-meta"><span>${esc(n.source||'来源未知')} · ${fmtDate(n.published_at)}</span>${n.url?`<a href="${esc(n.url)}" target="_blank" rel="noopener">查看来源 ↗</a>`:''}</div></article>`}function renderList(id,arr){$(id).innerHTML=(arr||[]).map(newsCard).join('')||'<div class="news-card">暂无数据</div>'}function marketMini(m){const cls=m.change_pct>0?'up':m.change_pct<0?'down':'flat';const arrow=m.change_pct>0?'▲':m.change_pct<0?'▼':'•';return `<div class="market-mini"><div class="market-name">${esc(m.name)}</div><div class="market-price">${esc(m.price)} ${esc(m.unit||'')}</div><div class="market-change ${cls}">${arrow} ${Math.abs(m.change_pct||0).toFixed(2)}%</div></div>`}function marketCard(m){const cls=m.change_pct>0?'up':m.change_pct<0?'down':'flat';const arrow=m.change_pct>0?'▲':m.change_pct<0?'▼':'•';return `<div class="market-card"><div class="name">${esc(m.name)}</div><div class="price">${esc(m.price)} <small>${esc(m.unit||'')}</small></div><div class="change ${cls}">${arrow} ${(m.change_pct||0).toFixed(2)}%</div><div class="sub">${esc(m.symbol||'')} · ${esc(m.note||'延迟行情')}</div></div>`}function render(data){state.data=data;$('#updatedAt').textContent=`最近更新：${fmtDate(data.updated_at)}（北京时间）`;$('#marketTime').textContent=`更新 ${fmtDate(data.markets_updated_at||data.updated_at)}`;$('#top5').innerHTML=(data.top5||[]).slice(0,5).map((x,i)=>`<article class="top-item"><div class="top-num">${i+1}</div><div><div class="top-title">${esc(x.title)}</div><div class="top-reason">${esc(x.why_it_matters||'')}</div></div></article>`).join('');renderList('#chinaPreview',(data.sections.china||[]).slice(0,4));renderList('#worldPreview',(data.sections.world||[]).slice(0,4));renderList('#aiPreview',(data.sections.ai||[]).slice(0,4));for(const key of ['china','world','finance','tech','society','ai'])renderList(`#${key}List`,data.sections[key]||[]);$('#marketStrip').innerHTML=(data.markets||[]).slice(0,6).map(marketMini).join('');$('#marketsGrid').innerHTML=(data.markets||[]).map(marketCard).join('')}async function load(){$('#updatedAt').textContent='正在读取最新数据…';try{const r=await fetch(`./data/news.json?t=${Date.now()}`);if(!r.ok)throw new Error('读取失败');render(await r.json())}catch(e){$('#updatedAt').textContent='数据读取失败，请稍后刷新'}}$$('.tab').forEach(btn=>btn.addEventListener('click',()=>{$$('.tab').forEach(x=>x.classList.remove('active'));$$('.tab-panel').forEach(x=>x.classList.remove('active'));btn.classList.add('active');document.querySelector(`[data-panel="${btn.dataset.tab}"]`).classList.add('active');window.scrollTo({top:0,behavior:'smooth'})}));$('#refreshBtn').addEventListener('click',load);const now=new Date();$('#todayDate').textContent=new Intl.DateTimeFormat('zh-CN',{year:'numeric',month:'long',day:'numeric',weekday:'long',timeZone:'Asia/Shanghai'}).format(now);load();